server {
    listen 80;
    server_name dev.${APP_DOMAIN};

    root ${FRONTEND_PANEL_ROOT};
    index index.html index.htm;

    location / {
        try_files ${DOLLAR}uri ${DOLLAR}uri/ /index.html;
    }
}
