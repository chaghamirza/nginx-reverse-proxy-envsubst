server {
    listen 80;
    server_name admin.${APP_DOMAIN};

    root ${FRONTEND_ADMIN_ROOT};
    index index.html index.htm;

    location / {
        try_files ${DOLLAR}uri ${DOLLAR}uri/ /index.html;
    }
}
